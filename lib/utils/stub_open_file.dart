// // Stub for open_file on Flutter Web.
// // On web, OpenFile.open is never called (guarded by kIsWeb checks),
// // but the stub is required so the code compiles for the web target.

// class OpenFile {
//   static Future<void> open(String filePath) async {
//     throw UnimplementedError('open_file is not available on web');
//   }
// }
