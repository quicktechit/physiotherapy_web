// import 'package:flutter/foundation.dart';

// /// Debug-only logger. Outputs nothing in release builds.
// /// Use this instead of print() throughout the app.
// void appLog(Object? message) {
//   if (kDebugMode) {
//     debugPrint(message?.toString());
//   }
// }
